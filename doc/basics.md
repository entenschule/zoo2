# basics


## installation

``` 
tilman@t570:~/learn_py/zoo2$ sudo apt install python3.8-venv
tilman@t570:~/learn_py/zoo2$ python3 -m venv env
tilman@t570:~/learn_py/zoo2$ source env/bin/activate
(env) tilman@t570:~/learn_py/zoo2$
```


``` 
pip install pytest
pip install bottle
```


## git

``` 
git remote add origin git@github.com:entenschule/zoo2.git
git branch -M main
git push -u origin main
```

``` 
git status                         # check if changes are committed
git config --global user.name      # check user if in doubt
git push
```
